@AGENTS.md

# <Project name>

## Specific to Claude Code

- Rules and skills: `.claude/rules/`, `.claude/skills/`
- Sub-agents: `.claude/agents/` (`code-reviewer`, `test-writer`)
- Active output style: `<style name>` (`.claude/styles/<style name>.md`)
